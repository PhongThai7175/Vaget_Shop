<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../partials/head.ejs'); %>

    <link rel="stylesheet" type="text/css" href="css/form.css">
    <title>Login</title>

  
</head>
<body>
  <div class="background">
    <div class="login">
      <form action="/login" method="post">
        <div class="content row">
            <div class="col-12 title">
              <h2>Login</h2>
            </div>
          <% if (error) { %>
            <div  class="alert alert-danger py-0" role="alert">
              <p class="text-center my-0"><%= error %></p>
            </div>
          <% } %> 
          <% if (user != 0) { %>
            <div class="col-12 form-group login-input">
              <lable class="form-label">Username:</lable>
              <input 
                name="email"
                placeholder="Enter your username"
                type="text" 
                class="form-control"
                required
                value="<%= user %>"
              />
          </div>
          <% } else  {%> 
            <div class="col-12 form-group login-input">
                <lable class="form-label">Username:</lable>
                <input 
                  name="email"
                  placeholder="Enter your username"
                  type="text" 
                  class="form-control"
                  required
                />
            </div>
            <% }%> 
            <div class="col-12 form-group login-input">
                <lable class="form-label">Password:</lable>
                <div class="custom-input-password">
                <input  
                  name="password"
                  placeholder="Enter your password" 
                  class="form-control"
                  type="password"
                  required
                 />
                 <a class="forgot-password" href="#">Forgot your password?</a>
                 
                </div>
                <a class="forgot-password" href="/sign-up">Register Now</a>
            </div>

            </div>
            <div class="col-12 text-center">
                <button class="btn btn-login btn-form" type="submit">Login</button>
                
            </div>
            <div class="col-12 text-center mt-3">
                <span class="text-orther-login">Login with</span>
            </div>
            <div class="col-12 social-login">
                <i class="fab fa-facebook-f social-icon"></i>
                <i class="fab fa-google-plus-g social-icon"></i>
            </div>
        </div>
      </form>
    </div>
  </div>
</body>
</html>