<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.">
    <%- include('../partials/head.ejs'); %>

    <link rel="stylesheet" type="text/css" href="css/homepage.css">


    <title>Home Page</title>
</head>
<body>

  <!-- Navigation Bar -->

  <%- include('../homepage/navbar.ejs'); %>
  

    <!-- JUMBOTRON -->
  <section class="main">
    <div class="container">
        <h1>farm fresh to you</h1>
        <p>We bring fresh vegetables to your door</p>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <!-- add something later -->

  <!-- PICK UP A BOX -->
  <section class="pickbox">
    <div class="container">
      <h1 class="text-white text-center">Our Products</h1>
      <div class="row text-center">

        <% if(product) { %>
          <% let length = product.length %>
          <% for (let i=0; i < length; i++) { %>
        <div class="col-md-3 mx-3 my-3 col-sm-6">
          <div class="card h-100">
              <img class="card-img-top" src="data:image/png;base64,<%= product[i].image %>">
              <div class="card-body d-flex flex-column">
                <h4 class="card-title"><%=product[i].name%></h4>
                <p class="card-text"><%=product[i].description%></p>
                <a class="btn mt-auto" href="/products?id=<%= product[i].id %>">Bring me this box</a>
              </div>

          </div>
        </div>
          <% }  %>
        <% } %>

      </div>
    </div>
  </section>


  <%- include('../partials/footer.ejs'); %>
  
</body>
</html>